services:
  topology-map:
    build: .
    image: ${TOPOLOGY_IMAGE:-gpu-fabric-topology:local}
    ports:
      - "${TOPOLOGY_BIND:-0.0.0.0}:${TOPOLOGY_PORT:-43147}:80"
    restart: unless-stopped
    extra_hosts:
      - "topology-host:192.168.1.247"
